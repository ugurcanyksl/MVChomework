@model List<SubCategory>
@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
    TempData["Title"] = "Alt Kategoriler";
}

<div class="col-lg-12">
    <div class="card">
        <div class="card-header">
            @Html.ActionLink("AltKategori Ekle", "AddSubCategory", "SubCategory", null, new { @class = "btn btn-info float-right" })
        </div>
        <div class="panel-heading">
            @if (TempData["Message"] != null)
            {
                <span class="alert-dismissible alert-success">@TempData["Message"]</span>
            }
            else
            {
                <h2></h2>
            }

        </div>
        <!-- /.card-header -->
        <div class="card-body">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>Alt Kategori Adı</th>
                        <th>Açıklama</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (SubCategory subCategory in Model)
                    {
                        <tr>
                            <td>@subCategory.SubCategoryName</td>
                            <td>@subCategory.Description</td>
                            <td>
                                @Html.ActionLink("Güncelle", "UpdateSubCategory", new { id = subCategory.ID }, new { @class = "badge bg-warning" })
                                @Html.ActionLink("Sil", "DeleteSubCategory", new { id = subCategory.ID }, new { @class = "badge bg-danger" })
                                
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

